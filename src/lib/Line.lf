target C {
    platform: "Pico",
    threading: false
}

preamble {=
    #include <pico/stdlib.h>
    #include <pololu_3pi_2040_robot.h>
=}

reactor Line {
    output reflect:int;
    timer t1(0, 100 msec);
    reaction(startup) {=
        // calibrate
    =}
    reaction(t1) {=
        
    =}
}