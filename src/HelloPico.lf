target C {
    platform: {
        name: "RP2040",
    },
    threading: false,
}

preamble {=
    #include <stdio.h>
    #include "pico/stdlib.h"
=}

main reactor {
    timer t1(0, 1 sec); 
    reaction(t1) {=
        printf("Hello World!\n");
    =}
}
