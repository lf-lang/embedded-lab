if (DEFINED ENV{ROBOT_LIB_PATH})
  set(ROBOT_LIB $ENV{ROBOT_LIB_PATH})
  message(ROBOT_LIB="${ROBOT_LIB}")
endif()

add_subdirectory(${ROBOT_LIB} pololu_3pi_2040_robot)
target_link_libraries(${LF_MAIN_TARGET} pololu_3pi_2040_robot)