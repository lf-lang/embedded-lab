set(ROBOT_LIB_DIR "./lib/robot-lib")
if (EXISTS ${ROBOT_LIB_DIR})
  add_subdirectory(${ROBOT_LIB_DIR} robot_lib)
  target_link_libraries(${LF_MAIN_TARGET} robot)
else()
  message("robot-lib not found, copy into src-gen directory")
  return()
endif()
